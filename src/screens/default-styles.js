import { View } from "react-native";
import styled from "styled-components";

export const FlexView = styled(View)`
  display: flex;
`;

export const FlexRowView = styled(FlexView)`
  flex-direction: row;
`;

export const Flex = styled(View)`
  flex: 1;
`;
